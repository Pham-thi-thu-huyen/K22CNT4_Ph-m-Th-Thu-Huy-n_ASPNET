@model IEnumerable<lesson08.ptth.Models.KHOA>

@{
    ViewBag.Title = "PtthIndex";
}

<h2>PtthIndex</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PtthTenKH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PtthTenKH)
        </td>
        <td>
            @Html.ActionLink("Edit", "PtthEdit", new { id=item.PtthMaKH.Trim()}) |
            @Html.ActionLink("Details", "PtthDetails", new { id=item.PtthMaKH.Trim() }) |
            @Html.ActionLink("Delete", "PtthDelete", new { id=item.PtthMaKH.Trim() })
        </td>
    </tr>
}

</table>
